@prelude(script)

// Conformance: transitive effect checking (check-pass only)
// Ensures main! properly declares Console when calling Console.println!

fn main!() -> {Console} () = {
  let x = 42
  Console.println!("Value: ${x}")
}
