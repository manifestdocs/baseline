@prelude(script)

// Negative test: explicit effect declaration violated
// Expected errors: CAP_001

// This function declares {Http} but calls Console (which is not declared)
fn bad_print() -> {Http} () = Console.println!("oops")
