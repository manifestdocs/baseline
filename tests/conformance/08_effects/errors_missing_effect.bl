@prelude(script)

// Negative test: missing effect annotation
// Expected errors: CAP_001

// This function calls Console but does not declare the effect
fn bad_print() -> () = Console.println!("oops")
