@prelude(script)

// Conformance: Console effect and annotation (check-pass only)
// Files with effects and main! are tested as check-pass only

// Pure function — no effect annotation needed
fn double(x: Int) -> Int = x * 2

fn main!() -> {Console} () = {
  let n = double(21)
  Console.println!("${n}")
}
