@prelude(server)

// Negative test: calling Sqlite without declaring {Sqlite} effect
// Expected errors: CAP_001

fn bad_query!() -> {Console} List<Map<String, String>> =
  Sqlite.query!("SELECT 1", [])
