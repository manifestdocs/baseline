@prelude(server)

// Sqlite module type checking — positive tests

fn main!() -> {Sqlite, Console} () = {
  Sqlite.connect!(":memory:")
  Sqlite.execute!("CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)", [])
  Sqlite.execute!("INSERT INTO users (name) VALUES (?1)", ["Alice"])
  let users = Sqlite.query!("SELECT id, name FROM users", [])
  Console.println!("ok")
}

// expect: ok
