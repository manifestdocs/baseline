@prelude(core)

fn safe_divide(a: Int, b: Int) -> Option<Int> =
  if b == 0 then None
  else Some(a / b)

fn main() -> () = {
  let result = safe_divide(10, 3)
  let msg = match result
    Some(v) -> "Result: " ++ Int.to_string(v)
    None -> "Division by zero"
  let _ = Console.println!(msg)
}
