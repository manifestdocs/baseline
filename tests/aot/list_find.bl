@prelude(core)

fn gt1(x: Int) -> Boolean = x > 1

fn format_option(opt: Option<Int>) -> String = {
  match opt
    Some(x) -> "Some(${x})"
    None -> "None"
}

fn main() -> String = {
  let result = List.find([1, 2, 3], gt1)
  format_option(result)
}
