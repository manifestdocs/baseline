@prelude(core)

fn main() -> Int = {
  let some_val = Some(42)
  let none_val = None
  let a = Option.unwrap(some_val)
  let b = Option.unwrap_or(none_val, 10)
  let c = if Option.is_some(some_val) then 1 else 0
  let d = if Option.is_none(none_val) then 1 else 0
  a + b + c + d
}
