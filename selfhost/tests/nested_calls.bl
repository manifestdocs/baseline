// Test: nested function calls and conditionals
// Expected exit code: 10

fn max(a: Int, b: Int) -> Int =
  if a > b then a else b

fn min(a: Int, b: Int) -> Int =
  if a < b then a else b

fn main() -> Int = {
  let a = max(0, min(100, 10))
  a
}
